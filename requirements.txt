# Core runtime
pydantic>=2.5
playwright>=1.40
Flask>=3.0
PyYAML>=6.0

# LLM provider (production / unattended discovery path). Not needed for the
# offline discovery reproduction or for replay.
anthropic>=0.39

# Dev / tests
pytest>=8.0
